Working Taskbar Clock: Update the taskbar tray clock (12:00 PM) in real-time using 
setInterval() and the JavaScript Date object.

Draggable Windows: Add a drag-and-drop event listener to the .ret-01__win title bars so 
users can click and drag the conference windows around the desktop.

Minimize & Close Buttons: Make the minimize (_) and close (✕) window buttons actually hide 
or close the windows, updating the active taskbar items dynamically.

Start Menu Toggle: Enhance or control the checkbox/Start menu behavior via JavaScript to 
handle clicks outside the menu to close it automatically.

Retro Sound Effects: Play classic Windows 95 WAV sound effects (like error pops or clicks) 
using the HTML5 Audio API when buttons or error windows are clicked.